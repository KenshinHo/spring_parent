server:
  port: 9001
spring:
  application:
    name: spring-base #指定服务名
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/tensquare_base?characterEncoding=utf-8
    username: tensquare
    password: tensquare
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true